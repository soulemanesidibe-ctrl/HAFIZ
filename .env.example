# Configuration Supabase (optionnelle)
# Renseigne ces deux variables pour activer les comptes utilisateurs
# et la synchronisation cloud de la progression.
# Si elles restent vides, l'app fonctionne en mode local (localStorage) — comportement par défaut.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
